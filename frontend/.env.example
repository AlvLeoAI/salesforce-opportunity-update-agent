# Base URL of the backend API. Defaults to http://localhost:8000 when unset.
# In production (Vercel) set this to the deployed Railway URL, e.g.
#   VITE_API_URL=https://opportunity-agent.up.railway.app
VITE_API_URL=http://localhost:8000
