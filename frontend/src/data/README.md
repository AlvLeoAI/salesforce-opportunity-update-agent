# Frontend Data

These JSON fixtures are precomputed demo outputs copied from `backend/outputs/` after running:

```bash
python3 backend/run_agent.py --all
```

The frontend is static and does not call a backend API.
