# Required for live LLM agent execution
OPENAI_API_KEY=your-openai-api-key-here

# Optional: override the default model (default: gpt-4o)
# OPENAI_MODEL=gpt-4o

# Optional: force simulator mode even with API key set
# SALESFORCE_AGENT_FORCE_FALLBACK=true
